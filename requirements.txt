# n8n MCP Server Requirements
# Install with: pip install -r requirements.txt

# Core MCP and FastMCP
mcp>=1.0.0

# HTTP client
httpx>=0.27.0

# Optional: For environment variable loading
python-dotenv>=1.0.0

# HTTP server support (for n8n_http_server.py)
uvicorn>=0.30.0
